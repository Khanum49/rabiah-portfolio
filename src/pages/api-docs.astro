---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="API Documentation - Rabiah Khanum">
  <div class="min-h-screen bg-gradient-to-br from-slate-50 via-white to-teal-50">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 py-12 sm:py-16">

      <!-- Page Header -->
      <div class="mb-10 sm:mb-8">
        <div class="inline-flex items-center gap-2 text-xs font-semibold tracking-widest text-teal-600 uppercase mb-4">
        </div>
        <h1 class="text-4xl sm:text-5xl font-bold text-slate-900 mb-4 leading-tight" style="font-family: 'Playfair Display', serif;">
          IoT Device Management API
        </h1>
        <p class="text-base sm:text-md text-slate-600 max-w-6xl leading-relaxed">
          A REST API reference for registering and managing embedded IoT devices, with live, testable endpoints and a structure based on Postman's API documentation best practices.
        </p>
        <div class="flex flex-wrap gap-2 mt-5">
          <span class="px-3 py-1 text-xs font-medium bg-teal-100 text-teal-700 rounded-full">REST API</span>
          <span class="px-3 py-1 text-xs font-medium bg-blue-100 text-blue-700 rounded-full">JSON</span>
          <span class="px-3 py-1 text-xs font-medium bg-purple-100 text-purple-700 rounded-full">Portfolio Sample</span>
          <span class="px-3 py-1 text-xs font-medium bg-amber-100 text-amber-700 rounded-full">Live Testable</span>
        </div>
      </div>

      <!-- Sample notice banner -->
      <div class="mb-8 flex items-start gap-3 bg-amber-50 border border-amber-200 rounded-xl p-4 text-sm">
        <svg class="w-5 h-5 text-amber-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path></svg>
        <p class="text-amber-800 leading-relaxed">
          <strong>This is a portfolio documentation sample.</strong> The IoT Device Management API is fictional and exists to demonstrate REST API documentation skills. The base URL will not resolve. The "Try it" demos use <a href="https://jsonplaceholder.typicode.com" target="_blank" class="underline font-medium">JSONPlaceholder</a> as a live mock backend so all responses are real HTTP calls.
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-4 gap-8">

        <!-- Sidebar nav -->
        <aside class="lg:col-span-1">
          <div class="sticky top-6 bg-white rounded-xl border border-slate-200 overflow-hidden shadow-sm">
            <div class="px-4 py-3 bg-gradient-to-r from-teal-500 to-teal-600">
              <p class="text-xs font-semibold text-white uppercase tracking-wider">On This Page</p>
            </div>
            <nav class="p-3 space-y-0.5 text-sm">
              <a href="#introduction" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">Introduction</a>
              <a href="#getting-started" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">Getting Started</a>
              <a href="#authentication" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">Authentication</a>

              <p class="px-3 pt-3 pb-1 text-xs font-semibold text-slate-400 uppercase tracking-wider">Devices</p>
              <a href="#get-devices" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">
                <span class="text-xs font-bold text-emerald-600 font-mono">GET</span> List Devices
              </a>
              <a href="#get-device-id" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">
                <span class="text-xs font-bold text-emerald-600 font-mono">GET</span> Get Device
              </a>
              <a href="#post-device" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">
                <span class="text-xs font-bold text-blue-600 font-mono">POST</span> Register Device
              </a>

              <p class="px-3 pt-3 pb-1 text-xs font-semibold text-slate-400 uppercase tracking-wider">Reference</p>
              <a href="#errors" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">Error Codes</a>
              <a href="#toolchain" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-slate-700 hover:bg-teal-50 hover:text-teal-700 transition-colors">Toolchain</a>
            </nav>
          </div>
        </aside>

        <!-- Main content -->
        <main class="lg:col-span-3 space-y-10">

          <!-- 1. Introduction -->
          <section id="introduction" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100">
              <h2 class="text-xl font-bold text-slate-900" style="font-family: 'Playfair Display', serif;">Introduction</h2>
            </div>
            <div class="px-6 py-6 space-y-5 text-slate-700 text-sm leading-relaxed">
              <p>
                The <strong>IoT Device Management API</strong> lets firmware engineers and platform teams register, query, and monitor embedded devices deployed in the field. It is designed for teams managing fleets of ESP32, STM32, or other ARM Cortex-M microcontrollers that report telemetry to a central backend.
              </p>
              <p>
                Use this API to bring a new device online, retrieve its current status, or list all devices in your project by filter. It follows REST conventions: resources are nouns, actions are HTTP methods, and every response is JSON.
              </p>

              <!-- What you can build -->
              <div class="bg-slate-50 border border-slate-200 rounded-xl p-5">
                <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-3">What you can build with this API</p>
                <ul class="space-y-2 text-sm text-slate-700">
                  <li class="flex items-start gap-2">
                    <span class="text-teal-500 mt-0.5">&#8594;</span>
                    <span>A fleet dashboard that shows all online devices and their firmware versions in real time</span>
                  </li>
                  <li class="flex items-start gap-2">
                    <span class="text-teal-500 mt-0.5">&#8594;</span>
                    <span>An onboarding script that registers new devices automatically after physical provisioning</span>
                  </li>
                  <li class="flex items-start gap-2">
                    <span class="text-teal-500 mt-0.5">&#8594;</span>
                    <span>An alerting system that polls device status and flags units that have gone offline</span>
                  </li>
                </ul>
              </div>

              <!-- API at a glance -->
              <div class="grid grid-cols-2 gap-4">
                <div class="bg-slate-50 rounded-xl p-4 border border-slate-200">
                  <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1">Base URL</p>
                  <code class="text-xs font-mono text-teal-700">api.iotfleet.example.com/v1</code>
                </div>
                <div class="bg-slate-50 rounded-xl p-4 border border-slate-200">
                  <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1">Protocol</p>
                  <p class="text-sm font-medium text-slate-800">HTTPS only</p>
                </div>
                <div class="bg-slate-50 rounded-xl p-4 border border-slate-200">
                  <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1">Response Format</p>
                  <p class="text-sm font-medium text-slate-800">JSON</p>
                </div>
                <div class="bg-slate-50 rounded-xl p-4 border border-slate-200">
                  <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1">Rate Limit</p>
                  <p class="text-sm font-medium text-slate-800">1,000 req / hour</p>
                </div>
              </div>
            </div>
          </section>

          <!-- 2. Getting Started -->
          <section id="getting-started" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100">
              <h2 class="text-xl font-bold text-slate-900" style="font-family: 'Playfair Display', serif;">Getting Started</h2>
            </div>
            <div class="px-6 py-6 space-y-5 text-slate-700 text-sm leading-relaxed">
              <p>
                Follow these steps to make your first successful API call. You should be able to retrieve a list of devices within five minutes of getting your token.
              </p>

              <ol class="space-y-4">
                <li class="flex gap-4">
                  <span class="flex-shrink-0 w-7 h-7 rounded-full bg-teal-600 text-white text-xs font-bold flex items-center justify-center">1</span>
                  <div>
                    <p class="font-semibold text-slate-800 mb-1">Create an account and generate an API token</p>
                    <p class="text-slate-600">Sign in to the IotFleet dashboard, navigate to <strong>Settings &rarr; API Tokens</strong>, and create a new token scoped to your project. Copy the token — it will not be shown again.</p>
                  </div>
                </li>
                <li class="flex gap-4">
                  <span class="flex-shrink-0 w-7 h-7 rounded-full bg-teal-600 text-white text-xs font-bold flex items-center justify-center">2</span>
                  <div>
                    <p class="font-semibold text-slate-800 mb-1">Make your first request</p>
                    <p class="text-slate-600 mb-3">Run the following command, replacing <code class="bg-slate-100 px-1 rounded font-mono">YOUR_API_TOKEN</code> with the token you just generated. A successful response returns a <code class="bg-slate-100 px-1 rounded font-mono">200 OK</code> with a JSON list of devices.</p>
                    <div class="bg-slate-900 rounded-xl overflow-hidden">
                      <div class="flex justify-end px-4 py-2 bg-slate-800">
                        <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="quickstart-curl">Copy</button>
                      </div>
                      <pre id="quickstart-curl" class="px-4 pb-4 text-sm font-mono text-slate-100 overflow-x-auto"><span class="text-teal-400">curl</span> -X GET <span class="text-yellow-300">"https://api.iotfleet.example.com/v1/devices"</span> \
  -H <span class="text-yellow-300">"Authorization: Bearer YOUR_API_TOKEN"</span></pre>
                    </div>
                  </div>
                </li>
                <li class="flex gap-4">
                  <span class="flex-shrink-0 w-7 h-7 rounded-full bg-teal-600 text-white text-xs font-bold flex items-center justify-center">3</span>
                  <div>
                    <p class="font-semibold text-slate-800 mb-1">Register your first device</p>
                    <p class="text-slate-600">Once you can list devices, use the <a href="#post-device" class="text-teal-600 underline">Register Device</a> endpoint to add your first ESP32 or STM32 unit to the fleet. From there, the <a href="#get-device-id" class="text-teal-600 underline">Get Device</a> endpoint lets you inspect its status and configuration at any time.</p>
                  </div>
                </li>
              </ol>
            </div>
          </section>

          <!-- 3. Authentication -->
          <section id="authentication" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100">
              <h2 class="text-xl font-bold text-slate-900" style="font-family: 'Playfair Display', serif;">Authentication</h2>
            </div>
            <div class="px-6 py-6 space-y-4 text-slate-700 text-sm leading-relaxed">
              <p>
                Every request must include a <strong>Bearer token</strong> in the <code class="font-mono bg-slate-100 px-1.5 py-0.5 rounded text-slate-800">Authorization</code> header. Tokens are project-scoped and expire after 90 days. There is no public access — unauthenticated requests return <code class="font-mono bg-slate-100 px-1.5 py-0.5 rounded text-slate-800">401 Unauthorized</code>.
              </p>

              <div class="bg-slate-900 rounded-xl overflow-hidden">
                <div class="flex items-center justify-between px-4 py-2 bg-slate-800">
                  <span class="text-xs text-slate-400 font-mono">Required header — every request</span>
                  <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="auth-example">Copy</button>
                </div>
                <pre id="auth-example" class="px-4 py-4 text-sm font-mono text-slate-100 overflow-x-auto"><span class="text-slate-400">Authorization:</span> <span class="text-teal-400">Bearer YOUR_API_TOKEN</span></pre>
              </div>

              <div class="border border-slate-200 rounded-xl overflow-hidden">
                <table class="w-full text-xs">
                  <thead class="bg-slate-50">
                    <tr>
                      <th class="text-left px-4 py-3 font-semibold text-slate-600">Property</th>
                      <th class="text-left px-4 py-3 font-semibold text-slate-600">Value</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-slate-100">
                    <tr>
                      <td class="px-4 py-3 text-slate-700">Auth scheme</td>
                      <td class="px-4 py-3 font-mono text-slate-800">Bearer token</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3 text-slate-700">Header name</td>
                      <td class="px-4 py-3 font-mono text-slate-800">Authorization</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3 text-slate-700">Token scope</td>
                      <td class="px-4 py-3 text-slate-600">Per project</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3 text-slate-700">Token expiry</td>
                      <td class="px-4 py-3 text-slate-600">90 days</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3 text-slate-700">Where to generate</td>
                      <td class="px-4 py-3 text-slate-600">Dashboard &rarr; Settings &rarr; API Tokens</td>
                    </tr>
                  </tbody>
                </table>
              </div>

              <div class="flex items-start gap-3 bg-amber-50 border border-amber-200 rounded-xl p-4">
                <svg class="w-5 h-5 text-amber-500 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path></svg>
                <p class="text-sm text-amber-800">Never embed your API token in client-side code or commit it to version control. Store it in an environment variable and reference it at runtime.</p>
              </div>
            </div>
          </section>

          <!-- Devices section divider -->
          <div class="flex items-center gap-3 pt-2">
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-widest">Devices</span>
            <div class="flex-1 h-px bg-slate-200"></div>
          </div>

          <!-- 4. GET /devices -->
          <section id="get-devices" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100 flex items-center gap-3 flex-wrap">
              <span class="px-2.5 py-1 text-xs font-bold rounded-lg bg-emerald-100 text-emerald-700 font-mono">GET</span>
              <code class="text-sm font-mono text-slate-700">/devices</code>
              <span class="ml-auto text-xs text-slate-500 italic">List all registered devices</span>
            </div>
            <div class="px-6 py-6 space-y-6 text-sm">

              <!-- Consumer-facing description -->
              <div class="space-y-2 text-slate-700 leading-relaxed">
                <p>Returns a paginated list of all devices registered in your project. Use this endpoint to build dashboards, run status checks, or identify devices that need a firmware update.</p>
                <p>Results are ordered by registration date, newest first. Use the <code class="bg-slate-100 px-1 rounded font-mono">status</code> filter to narrow results to devices that are currently <code class="bg-slate-100 px-1 rounded font-mono">online</code>, <code class="bg-slate-100 px-1 rounded font-mono">offline</code>, or in an <code class="bg-slate-100 px-1 rounded font-mono">error</code> state.</p>
              </div>

              <!-- Query params -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-3">Query Parameters</h4>
                <div class="border border-slate-200 rounded-xl overflow-hidden">
                  <table class="w-full text-xs">
                    <thead class="bg-slate-50">
                      <tr>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Parameter</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Type</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Required</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Description</th>
                      </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">status</td>
                        <td class="px-4 py-3 text-slate-600">string</td>
                        <td class="px-4 py-3 text-slate-500">No</td>
                        <td class="px-4 py-3 text-slate-600">Filter by device status. Accepted values: <code class="bg-slate-100 px-1 rounded">online</code>, <code class="bg-slate-100 px-1 rounded">offline</code>, <code class="bg-slate-100 px-1 rounded">error</code>. Omit to return all devices.</td>
                      </tr>
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">limit</td>
                        <td class="px-4 py-3 text-slate-600">integer</td>
                        <td class="px-4 py-3 text-slate-500">No</td>
                        <td class="px-4 py-3 text-slate-600">Number of results per page. Default: <code class="bg-slate-100 px-1 rounded">20</code>. Maximum: <code class="bg-slate-100 px-1 rounded">100</code>.</td>
                      </tr>
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">page</td>
                        <td class="px-4 py-3 text-slate-600">integer</td>
                        <td class="px-4 py-3 text-slate-500">No</td>
                        <td class="px-4 py-3 text-slate-600">Page number for pagination. Starts at <code class="bg-slate-100 px-1 rounded">1</code>. Use alongside <code class="bg-slate-100 px-1 rounded">limit</code> to page through large fleets.</td>
                      </tr>
                    </tbody>
                  </table>
                </div>
              </div>

              <!-- Request example -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-3">Request Example</h4>
                <div class="rounded-xl overflow-hidden border border-slate-800">
                  <div class="flex bg-slate-800">
                    <button class="tab-btn active-tab px-4 py-2 text-xs font-medium text-white" data-tab="get-devices-curl">curl</button>
                    <button class="tab-btn px-4 py-2 text-xs font-medium text-slate-400 hover:text-white transition-colors" data-tab="get-devices-js">JavaScript</button>
                  </div>
                  <div id="get-devices-curl" class="tab-panel bg-slate-900">
                    <div class="flex justify-end px-4 py-2 bg-slate-800">
                      <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="get-devices-curl-code">Copy</button>
                    </div>
                    <pre id="get-devices-curl-code" class="px-4 pb-4 text-sm font-mono text-slate-100 overflow-x-auto"><span class="text-teal-400">curl</span> -X GET <span class="text-yellow-300">"https://api.iotfleet.example.com/v1/devices?status=online&limit=10"</span> \
  -H <span class="text-yellow-300">"Authorization: Bearer YOUR_API_TOKEN"</span> \
  -H <span class="text-yellow-300">"Content-Type: application/json"</span></pre>
                  </div>
                  <div id="get-devices-js" class="tab-panel hidden bg-slate-900">
                    <div class="flex justify-end px-4 py-2 bg-slate-800">
                      <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="get-devices-js-code">Copy</button>
                    </div>
                    <pre id="get-devices-js-code" class="px-4 pb-4 text-sm font-mono text-slate-100 overflow-x-auto"><span class="text-blue-400">const</span> response = <span class="text-blue-400">await</span> fetch(
  <span class="text-yellow-300">"https://api.iotfleet.example.com/v1/devices?status=online&limit=10"</span>,
  &#123;
    method: <span class="text-yellow-300">"GET"</span>,
    headers: &#123;
      <span class="text-yellow-300">"Authorization"</span>: <span class="text-yellow-300">`Bearer $&#123;apiToken&#125;`</span>,
      <span class="text-yellow-300">"Content-Type"</span>: <span class="text-yellow-300">"application/json"</span>
    &#125;
  &#125;
);
<span class="text-blue-400">const</span> data = <span class="text-blue-400">await</span> response.json();</pre>
                  </div>
                </div>
              </div>

              <!-- Response example — named -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-1">Response Example</h4>
                <p class="text-xs text-slate-500 mb-3">200 OK &mdash; Three online devices returned, newest first</p>
                <div class="bg-slate-900 rounded-xl overflow-hidden">
                  <div class="flex justify-end px-4 py-2 bg-slate-800">
                    <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="get-devices-response">Copy</button>
                  </div>
                  <pre id="get-devices-response" class="px-4 pb-4 text-sm font-mono text-slate-100 overflow-x-auto">&#123;
  <span class="text-teal-300">"total"</span>: 3,
  <span class="text-teal-300">"page"</span>: 1,
  <span class="text-teal-300">"devices"</span>: [
    &#123;
      <span class="text-teal-300">"id"</span>: <span class="text-yellow-300">"dev_a1b2c3"</span>,
      <span class="text-teal-300">"name"</span>: <span class="text-yellow-300">"sensor-node-01"</span>,
      <span class="text-teal-300">"chip"</span>: <span class="text-yellow-300">"ESP32-WROOM-32"</span>,
      <span class="text-teal-300">"firmware"</span>: <span class="text-yellow-300">"2.1.4"</span>,
      <span class="text-teal-300">"status"</span>: <span class="text-yellow-300">"online"</span>,
      <span class="text-teal-300">"last_seen"</span>: <span class="text-yellow-300">"2026-06-15T08:32:00Z"</span>
    &#125;
  ]
&#125;</pre>
                </div>
              </div>

              <!-- Live demo -->
              <div class="bg-gradient-to-br from-teal-50 to-blue-50 rounded-xl border border-teal-200 p-5">
                <div class="flex items-center justify-between mb-3">
                  <div>
                    <h4 class="font-semibold text-slate-800 text-sm">Try it live</h4>
                    <p class="text-xs text-slate-500 mt-0.5">Calls JSONPlaceholder and remaps the response to device objects. Returns a real HTTP response.</p>
                  </div>
                  <button id="try-get-devices" class="try-btn px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white text-xs font-semibold rounded-lg transition-colors flex items-center gap-2">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    Run request
                  </button>
                </div>
                <div id="result-get-devices" class="hidden bg-slate-900 rounded-lg p-4 overflow-x-auto">
                  <pre class="text-xs font-mono text-slate-100 result-text"></pre>
                </div>
              </div>
            </div>
          </section>

          <!-- 5. GET /devices/:id -->
          <section id="get-device-id" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100 flex items-center gap-3 flex-wrap">
              <span class="px-2.5 py-1 text-xs font-bold rounded-lg bg-emerald-100 text-emerald-700 font-mono">GET</span>
              <code class="text-sm font-mono text-slate-700">/devices/&#123;id&#125;</code>
              <span class="ml-auto text-xs text-slate-500 italic">Get a single device by ID</span>
            </div>
            <div class="px-6 py-6 space-y-6 text-sm">

              <div class="space-y-2 text-slate-700 leading-relaxed">
                <p>Returns the full record for a single device, including its last-known IP address, configuration flags, and telemetry interval. Use this endpoint when you need to inspect or display a specific device's details.</p>
                <p>The device <code class="bg-slate-100 px-1 rounded font-mono">id</code> is returned in every object from <a href="#get-devices" class="text-teal-600 underline">List Devices</a>. If the ID does not exist in your project, the API returns <code class="bg-slate-100 px-1 rounded font-mono">404 not_found</code>.</p>
              </div>

              <!-- Path params -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-3">Path Parameters</h4>
                <div class="border border-slate-200 rounded-xl overflow-hidden">
                  <table class="w-full text-xs">
                    <thead class="bg-slate-50">
                      <tr>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Parameter</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Type</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Required</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Description</th>
                      </tr>
                    </thead>
                    <tbody>
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">id</td>
                        <td class="px-4 py-3 text-slate-600">string</td>
                        <td class="px-4 py-3 text-emerald-600 font-semibold">Yes</td>
                        <td class="px-4 py-3 text-slate-600">The unique device identifier, e.g. <code class="bg-slate-100 px-1 rounded">dev_a1b2c3</code>. Returned by List Devices.</td>
                      </tr>
                    </tbody>
                  </table>
                </div>
              </div>

              <!-- Response example — named -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-1">Response Example</h4>
                <p class="text-xs text-slate-500 mb-3">200 OK &mdash; Full device record including config block</p>
                <div class="bg-slate-900 rounded-xl overflow-hidden">
                  <pre class="px-4 py-4 text-sm font-mono text-slate-100 overflow-x-auto">&#123;
  <span class="text-teal-300">"id"</span>: <span class="text-yellow-300">"dev_a1b2c3"</span>,
  <span class="text-teal-300">"name"</span>: <span class="text-yellow-300">"sensor-node-01"</span>,
  <span class="text-teal-300">"chip"</span>: <span class="text-yellow-300">"ESP32-WROOM-32"</span>,
  <span class="text-teal-300">"firmware"</span>: <span class="text-yellow-300">"2.1.4"</span>,
  <span class="text-teal-300">"status"</span>: <span class="text-yellow-300">"online"</span>,
  <span class="text-teal-300">"ip_address"</span>: <span class="text-yellow-300">"192.168.1.42"</span>,
  <span class="text-teal-300">"created_at"</span>: <span class="text-yellow-300">"2025-11-03T14:20:00Z"</span>,
  <span class="text-teal-300">"last_seen"</span>: <span class="text-yellow-300">"2026-06-15T08:32:00Z"</span>,
  <span class="text-teal-300">"config"</span>: &#123;
    <span class="text-teal-300">"telemetry_interval_s"</span>: 30,
    <span class="text-teal-300">"ota_enabled"</span>: <span class="text-blue-300">true</span>
  &#125;
&#125;</pre>
                </div>
              </div>

              <!-- Live demo -->
              <div class="bg-gradient-to-br from-teal-50 to-blue-50 rounded-xl border border-teal-200 p-5">
                <div class="flex items-center justify-between mb-3">
                  <div>
                    <h4 class="font-semibold text-slate-800 text-sm">Try it live</h4>
                    <p class="text-xs text-slate-500 mt-0.5">Fetches post ID 1 from JSONPlaceholder to simulate a single-device lookup.</p>
                  </div>
                  <button id="try-get-device" class="try-btn px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white text-xs font-semibold rounded-lg transition-colors flex items-center gap-2">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    Run request
                  </button>
                </div>
                <div id="result-get-device" class="hidden bg-slate-900 rounded-lg p-4 overflow-x-auto">
                  <pre class="text-xs font-mono text-slate-100 result-text"></pre>
                </div>
              </div>
            </div>
          </section>

          <!-- 6. POST /devices -->
          <section id="post-device" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100 flex items-center gap-3 flex-wrap">
              <span class="px-2.5 py-1 text-xs font-bold rounded-lg bg-blue-100 text-blue-700 font-mono">POST</span>
              <code class="text-sm font-mono text-slate-700">/devices</code>
              <span class="ml-auto text-xs text-slate-500 italic">Register a new device</span>
            </div>
            <div class="px-6 py-6 space-y-6 text-sm">

              <div class="space-y-2 text-slate-700 leading-relaxed">
                <p>Registers a new device in your project and assigns it a system-generated ID. The device must be physically provisioned and powered on before you call this endpoint — the API does not provision hardware, it records it.</p>
                <p>On success, the API returns <code class="bg-slate-100 px-1 rounded font-mono">201 Created</code> with the new device object. The initial <code class="bg-slate-100 px-1 rounded font-mono">status</code> will be <code class="bg-slate-100 px-1 rounded font-mono">provisioning</code> until the device makes its first telemetry ping, after which it transitions to <code class="bg-slate-100 px-1 rounded font-mono">online</code>.</p>
              </div>

              <!-- Request body -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-3">Request Body <span class="ml-2 text-xs font-normal text-slate-500 font-sans">Content-Type: application/json</span></h4>
                <div class="border border-slate-200 rounded-xl overflow-hidden">
                  <table class="w-full text-xs">
                    <thead class="bg-slate-50">
                      <tr>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Field</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Type</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Required</th>
                        <th class="text-left px-4 py-3 font-semibold text-slate-600">Description</th>
                      </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">name</td>
                        <td class="px-4 py-3 text-slate-600">string</td>
                        <td class="px-4 py-3 text-emerald-600 font-semibold">Yes</td>
                        <td class="px-4 py-3 text-slate-600">A human-readable label for the device. Maximum 64 characters. Used in dashboards and alerts.</td>
                      </tr>
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">chip</td>
                        <td class="px-4 py-3 text-slate-600">string</td>
                        <td class="px-4 py-3 text-emerald-600 font-semibold">Yes</td>
                        <td class="px-4 py-3 text-slate-600">The microcontroller identifier. Examples: <code class="bg-slate-100 px-1 rounded">ESP32-WROOM-32</code>, <code class="bg-slate-100 px-1 rounded">STM32F407</code>, <code class="bg-slate-100 px-1 rounded">ESP32-S3</code>.</td>
                      </tr>
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">firmware</td>
                        <td class="px-4 py-3 text-slate-600">string</td>
                        <td class="px-4 py-3 text-emerald-600 font-semibold">Yes</td>
                        <td class="px-4 py-3 text-slate-600">The firmware version currently flashed on the device. Must follow semantic versioning, e.g. <code class="bg-slate-100 px-1 rounded">2.1.4</code>.</td>
                      </tr>
                      <tr>
                        <td class="px-4 py-3 font-mono text-slate-800">ota_enabled</td>
                        <td class="px-4 py-3 text-slate-600">boolean</td>
                        <td class="px-4 py-3 text-slate-500">No</td>
                        <td class="px-4 py-3 text-slate-600">Whether the device should receive over-the-air firmware updates. Defaults to <code class="bg-slate-100 px-1 rounded">false</code>.</td>
                      </tr>
                    </tbody>
                  </table>
                </div>
              </div>

              <!-- Request example -->
              <div>
                <h4 class="font-semibold text-slate-800 mb-3">Request Example</h4>
                <div class="rounded-xl overflow-hidden border border-slate-800">
                  <div class="flex bg-slate-800">
                    <button class="tab-btn active-tab px-4 py-2 text-xs font-medium text-white" data-tab="post-curl">curl</button>
                    <button class="tab-btn px-4 py-2 text-xs font-medium text-slate-400 hover:text-white transition-colors" data-tab="post-js">JavaScript</button>
                  </div>
                  <div id="post-curl" class="tab-panel bg-slate-900">
                    <div class="flex justify-end px-4 py-2 bg-slate-800">
                      <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="post-curl-code">Copy</button>
                    </div>
                    <pre id="post-curl-code" class="px-4 pb-4 text-sm font-mono text-slate-100 overflow-x-auto"><span class="text-teal-400">curl</span> -X POST <span class="text-yellow-300">"https://api.iotfleet.example.com/v1/devices"</span> \
  -H <span class="text-yellow-300">"Authorization: Bearer YOUR_API_TOKEN"</span> \
  -H <span class="text-yellow-300">"Content-Type: application/json"</span> \
  -d <span class="text-yellow-300">'&#123;
    "name": "sensor-node-02",
    "chip": "ESP32-WROOM-32",
    "firmware": "2.1.4",
    "ota_enabled": true
  &#125;'</span></pre>
                  </div>
                  <div id="post-js" class="tab-panel hidden bg-slate-900">
                    <div class="flex justify-end px-4 py-2 bg-slate-800">
                      <button class="copy-btn text-xs text-slate-400 hover:text-white transition-colors" data-target="post-js-code">Copy</button>
                    </div>
                    <pre id="post-js-code" class="px-4 pb-4 text-sm font-mono text-slate-100 overflow-x-auto"><span class="text-blue-400">const</span> response = <span class="text-blue-400">await</span> fetch(
  <span class="text-yellow-300">"https://api.iotfleet.example.com/v1/devices"</span>,
  &#123;
    method: <span class="text-yellow-300">"POST"</span>,
    headers: &#123;
      <span class="text-yellow-300">"Authorization"</span>: <span class="text-yellow-300">`Bearer $&#123;apiToken&#125;`</span>,
      <span class="text-yellow-300">"Content-Type"</span>: <span class="text-yellow-300">"application/json"</span>
    &#125;,
    body: JSON.stringify(&#123;
      name: <span class="text-yellow-300">"sensor-node-02"</span>,
      chip: <span class="text-yellow-300">"ESP32-WROOM-32"</span>,
      firmware: <span class="text-yellow-300">"2.1.4"</span>,
      ota_enabled: <span class="text-blue-300">true</span>
    &#125;)
  &#125;
);
<span class="text-blue-400">const</span> data = <span class="text-blue-400">await</span> response.json();</pre>
                  </div>
                </div>
              </div>

              <!-- Response examples — named -->
              <div class="space-y-4">
                <h4 class="font-semibold text-slate-800">Response Examples</h4>

                <div>
                  <p class="text-xs text-slate-500 mb-2">201 Created &mdash; Device registered, awaiting first telemetry ping</p>
                  <div class="bg-slate-900 rounded-xl overflow-hidden">
                    <pre class="px-4 py-4 text-sm font-mono text-slate-100 overflow-x-auto">&#123;
  <span class="text-teal-300">"id"</span>: <span class="text-yellow-300">"dev_x9y8z7"</span>,
  <span class="text-teal-300">"name"</span>: <span class="text-yellow-300">"sensor-node-02"</span>,
  <span class="text-teal-300">"chip"</span>: <span class="text-yellow-300">"ESP32-WROOM-32"</span>,
  <span class="text-teal-300">"firmware"</span>: <span class="text-yellow-300">"2.1.4"</span>,
  <span class="text-teal-300">"status"</span>: <span class="text-yellow-300">"provisioning"</span>,
  <span class="text-teal-300">"created_at"</span>: <span class="text-yellow-300">"2026-06-15T10:05:00Z"</span>,
  <span class="text-teal-300">"ota_enabled"</span>: <span class="text-blue-300">true</span>
&#125;</pre>
                  </div>
                </div>

                <div>
                  <p class="text-xs text-slate-500 mb-2">400 Bad Request &mdash; Missing required field</p>
                  <div class="bg-slate-900 rounded-xl overflow-hidden">
                    <pre class="px-4 py-4 text-sm font-mono text-slate-100 overflow-x-auto">&#123;
  <span class="text-teal-300">"error"</span>: &#123;
    <span class="text-teal-300">"code"</span>: <span class="text-red-400">"bad_request"</span>,
    <span class="text-teal-300">"message"</span>: <span class="text-yellow-300">"Field 'chip' is required."</span>
  &#125;
&#125;</pre>
                  </div>
                </div>
              </div>

              <!-- Live demo POST -->
              <div class="bg-gradient-to-br from-teal-50 to-blue-50 rounded-xl border border-teal-200 p-5">
                <div class="flex items-center justify-between mb-3">
                  <div>
                    <h4 class="font-semibold text-slate-800 text-sm">Try it live</h4>
                    <p class="text-xs text-slate-500 mt-0.5">POSTs to JSONPlaceholder and remaps the response.</p>
                    <p class="text-xs text-slate-500 mt-0.5">No device is created, the mock API echoes a response as if it were.</p>
                  </div>
                  <button id="try-post-device" class="try-btn px-4 py-2 bg-teal-600 hover:bg-teal-700 text-white text-xs font-semibold rounded-lg transition-colors flex items-center gap-2">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    Run request
                  </button>
                </div>
                <div id="result-post-device" class="hidden bg-slate-900 rounded-lg p-4 overflow-x-auto">
                  <pre class="text-xs font-mono text-slate-100 result-text"></pre>
                </div>
              </div>
            </div>
          </section>

          <!-- Reference divider -->
          <div class="flex items-center gap-3 pt-2">
            <span class="text-xs font-semibold text-slate-400 uppercase tracking-widest">Reference</span>
            <div class="flex-1 h-px bg-slate-200"></div>
          </div>

          <!-- 7. Error Codes -->
          <section id="errors" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100">
              <h2 class="text-xl font-bold text-slate-900" style="font-family: 'Playfair Display', serif;">Error Codes</h2>
            </div>
            <div class="px-6 py-6 space-y-5 text-sm">
              <p class="text-slate-700 leading-relaxed">
                The API uses standard HTTP status codes. When a request fails, the response body always includes an <code class="font-mono bg-slate-100 px-1.5 py-0.5 rounded text-slate-800">error</code> object with a machine-readable <code class="font-mono bg-slate-100 px-1.5 py-0.5 rounded text-slate-800">code</code> and a human-readable <code class="font-mono bg-slate-100 px-1.5 py-0.5 rounded text-slate-800">message</code> explaining what went wrong.
              </p>

              <div class="bg-slate-900 rounded-xl overflow-hidden">
                <div class="px-4 py-2 bg-slate-800">
                  <span class="text-xs text-slate-400">Error response shape</span>
                </div>
                <pre class="px-4 py-4 text-sm font-mono text-slate-100">&#123;
  <span class="text-teal-300">"error"</span>: &#123;
    <span class="text-teal-300">"code"</span>: <span class="text-yellow-300">"not_found"</span>,
    <span class="text-teal-300">"message"</span>: <span class="text-yellow-300">"Device dev_a1b2c3 does not exist in this project."</span>
  &#125;
&#125;</pre>
              </div>

              <div class="border border-slate-200 rounded-xl overflow-hidden">
                <table class="w-full text-xs">
                  <thead class="bg-slate-50">
                    <tr>
                      <th class="text-left px-4 py-3 font-semibold text-slate-600">HTTP Status</th>
                      <th class="text-left px-4 py-3 font-semibold text-slate-600">Error Code</th>
                      <th class="text-left px-4 py-3 font-semibold text-slate-600">Meaning</th>
                      <th class="text-left px-4 py-3 font-semibold text-slate-600">Common Cause</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-slate-100">
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-emerald-600">200</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">—</td>
                      <td class="px-4 py-3 text-slate-600">Request succeeded</td>
                      <td class="px-4 py-3 text-slate-500">—</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-emerald-600">201</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">—</td>
                      <td class="px-4 py-3 text-slate-600">Resource created</td>
                      <td class="px-4 py-3 text-slate-500">Successful POST</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-amber-500">400</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">bad_request</td>
                      <td class="px-4 py-3 text-slate-600">Malformed request</td>
                      <td class="px-4 py-3 text-slate-500">Missing required field, invalid JSON</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-amber-500">401</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">unauthorized</td>
                      <td class="px-4 py-3 text-slate-600">Authentication failed</td>
                      <td class="px-4 py-3 text-slate-500">Missing, expired, or invalid API token</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-amber-500">404</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">not_found</td>
                      <td class="px-4 py-3 text-slate-600">Resource does not exist</td>
                      <td class="px-4 py-3 text-slate-500">Invalid device ID in path</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-red-500">429</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">rate_limited</td>
                      <td class="px-4 py-3 text-slate-600">Too many requests</td>
                      <td class="px-4 py-3 text-slate-500">Exceeded 1,000 requests/hour. Retry after the <code class="bg-slate-100 px-1 rounded">Retry-After</code> header value.</td>
                    </tr>
                    <tr>
                      <td class="px-4 py-3"><span class="font-mono font-bold text-red-500">500</span></td>
                      <td class="px-4 py-3 font-mono text-slate-700">server_error</td>
                      <td class="px-4 py-3 text-slate-600">Internal server error</td>
                      <td class="px-4 py-3 text-slate-500">Not your fault. Retry with exponential backoff.</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </div>
          </section>

          <!-- 8. Toolchain -->
          <section id="toolchain" class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
            <div class="px-6 py-5 border-b border-slate-100">
              <h2 class="text-xl font-bold text-slate-900" style="font-family: 'Playfair Display', serif;">Toolchain</h2>
            </div>
            <div class="px-6 py-6 text-sm text-slate-700 leading-relaxed space-y-4">
              <p>This documentation sample was structured following <a href="https://academy.postman.com/authoring-api-documentation" target="_blank" class="text-teal-600 underline">Postman's API documentation course</a>, which defines four qualities that make API docs worth reading: <strong>complete</strong>, <strong>accurate</strong>, <strong>clear</strong>, and <strong>discoverable</strong>. The structure maps directly to how a Postman collection would be organized — folders as section groups, requests in reading order, and named response examples per endpoint.</p>
              <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 pt-1">
                <div class="bg-slate-50 border border-slate-200 rounded-xl p-4 text-center">
                  <p class="font-semibold text-slate-800 text-xs">Postman</p>
                  <p class="text-xs text-slate-500 mt-1">Collection structure &amp; docs</p>
                </div>
                <div class="bg-slate-50 border border-slate-200 rounded-xl p-4 text-center">
                  <p class="font-semibold text-slate-800 text-xs">OpenAPI / Swagger</p>
                  <p class="text-xs text-slate-500 mt-1">Spec format reference</p>
                </div>
                <div class="bg-slate-50 border border-slate-200 rounded-xl p-4 text-center">
                  <p class="font-semibold text-slate-800 text-xs">JSONPlaceholder</p>
                  <p class="text-xs text-slate-500 mt-1">Mock backend for demos</p>
                </div>
                <div class="bg-slate-50 border border-slate-200 rounded-xl p-4 text-center">
                  <p class="font-semibold text-slate-800 text-xs">Redoc / ReadMe</p>
                  <p class="text-xs text-slate-500 mt-1">Rendering &amp; publishing</p>
                </div>
              </div>
            </div>
          </section>

        </main>
      </div>
    </div>
  </div>

  <script>
    // ── Tab switching ──────────────────────────────────────────────
    document.querySelectorAll('.tab-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const targetId = btn.dataset.tab;
        const tabBar = btn.parentElement;
        tabBar.querySelectorAll('.tab-btn').forEach(b => {
          b.classList.remove('active-tab', 'text-white');
          b.classList.add('text-slate-400');
        });
        btn.classList.add('active-tab', 'text-white');
        btn.classList.remove('text-slate-400');
        const section = tabBar.closest('.rounded-xl');
        section.querySelectorAll('.tab-panel').forEach(p => p.classList.add('hidden'));
        document.getElementById(targetId)?.classList.remove('hidden');
      });
    });

    // ── Copy buttons ──────────────────────────────────────────────
    document.querySelectorAll('.copy-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const target = document.getElementById(btn.dataset.target);
        if (!target) return;
        navigator.clipboard.writeText(target.innerText.trim()).then(() => {
          const orig = btn.textContent;
          btn.textContent = 'Copied!';
          setTimeout(() => (btn.textContent = orig), 1500);
        });
      });
    });

    // ── Live API demos ────────────────────────────────────────────
    async function runDemo(btnId, resultId, fetchFn) {
      const btn = document.getElementById(btnId);
      const resultBox = document.getElementById(resultId);
      const pre = resultBox.querySelector('.result-text');

      btn.disabled = true;
      btn.innerHTML = `<svg class="w-3.5 h-3.5 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg> Loading...`;
      resultBox.classList.remove('hidden');
      pre.textContent = 'Waiting for response...';

      try {
        const data = await fetchFn();
        pre.textContent = JSON.stringify(data, null, 2);
      } catch (e) {
        pre.textContent = `Error: ${e.message}`;
      } finally {
        btn.disabled = false;
        btn.innerHTML = `<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg> Run request`;
      }
    }

    document.getElementById('try-get-devices').addEventListener('click', () =>
      runDemo('try-get-devices', 'result-get-devices', async () => {
        const res = await fetch('https://jsonplaceholder.typicode.com/posts?_limit=3');
        const raw = await res.json();
        return {
          total: 3,
          page: 1,
          devices: raw.map((p, i) => ({
            id: `dev_${p.id.toString().padStart(6, '0')}`,
            name: `sensor-node-0${i + 1}`,
            chip: ['ESP32-WROOM-32', 'STM32F407', 'ESP32-S3'][i % 3],
            firmware: '2.1.4',
            status: 'online',
            last_seen: new Date().toISOString()
          }))
        };
      })
    );

    document.getElementById('try-get-device').addEventListener('click', () =>
      runDemo('try-get-device', 'result-get-device', async () => {
        const res = await fetch('https://jsonplaceholder.typicode.com/posts/1');
        const raw = await res.json();
        return {
          id: `dev_${String(raw.id).padStart(6, '0')}`,
          name: 'sensor-node-01',
          chip: 'ESP32-WROOM-32',
          firmware: '2.1.4',
          status: 'online',
          ip_address: '192.168.1.42',
          created_at: '2025-11-03T14:20:00Z',
          last_seen: new Date().toISOString(),
          config: { telemetry_interval_s: 30, ota_enabled: true }
        };
      })
    );

    document.getElementById('try-post-device').addEventListener('click', () =>
      runDemo('try-post-device', 'result-post-device', async () => {
        const res = await fetch('https://jsonplaceholder.typicode.com/posts', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ name: 'sensor-node-02', chip: 'ESP32-WROOM-32', firmware: '2.1.4', ota_enabled: true })
        });
        const raw = await res.json();
        return {
          id: `dev_x${raw.id}y8z7`,
          name: 'sensor-node-02',
          chip: 'ESP32-WROOM-32',
          firmware: '2.1.4',
          status: 'provisioning',
          created_at: new Date().toISOString(),
          ota_enabled: true
        };
      })
    );
  </script>
</BaseLayout>